---
import settings from '../data/settings.json';
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="wrap footer-inner">
    <div class="footer-grid">
      <div class="footer-about">
        <div class="footer-brand">
          <img src="/images/logo.png" alt={settings.companyName} width="120" />
        </div>
        <p>ConstructEng Australia Pty Ltd. Strategic recruitment in construction and engineering, Australia-wide.</p>
      </div>
      <div class="footer-col">
        <span class="footer-heading">Contact</span>
        <span class="footer-name">{settings.founderName}</span>
        <a href={`tel:${settings.phone}`}>{settings.phoneDisplay}</a>
        <a href={`mailto:${settings.email}`}>{settings.email}</a>
        <a href={settings.linkedinUrl} target="_blank" rel="noopener">LinkedIn ↗</a>
      </div>
      <div class="footer-col">
        <span class="footer-heading">Site</span>
        <a href="/about">About Us</a>
        <a href="/job-seekers">Current Vacancies</a>
        <a href="/testimonials">Testimonials</a>
        <a href="/employers">Employers</a>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© {year} ConstructEng Australia Pty Ltd. All rights reserved.</span>
      <a href="/privacy-policy">Privacy Policy</a>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--ink);
    color: #fff;
  }
  .footer-inner {
    padding: 64px 24px 32px;
  }
  .footer-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 40px;
    justify-content: space-between;
  }
  .footer-about {
    max-width: 320px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .footer-brand {
    background: var(--paper);
    padding: 14px 18px;
    display: inline-flex;
    width: fit-content;
  }
  .footer-about p {
    font-size: 14px;
    line-height: 1.6;
    color: var(--muted-on-dark);
  }
  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .footer-heading {
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--muted-on-dark);
    text-transform: uppercase;
    margin-bottom: 4px;
  }
  .footer-name {
    font-size: 14px;
    color: #fff;
    font-weight: 600;
  }
  .footer-col a {
    font-size: 14px;
    color: var(--muted-on-dark);
    text-decoration: none;
  }
  .footer-col a:hover {
    color: #fff;
  }
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    padding-top: 24px;
    margin-top: 48px;
    border-top: 1px solid var(--line-dark);
  }
  .footer-bottom span,
  .footer-bottom a {
    font-size: 13px;
    color: var(--muted-on-dark);
    text-decoration: none;
  }
</style>
